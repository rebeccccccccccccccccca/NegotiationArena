# Tested against Python 3.13.7 (see requirements.lock.txt for the exact
# resolved environment this was verified against).
openai
python-dotenv==1.0.0
matplotlib==3.7.3
anthropic==0.5.0
streamlit>=1.64
pandas
